/**
 * App constants
 * Update API_BASE_URL for your environment (e.g. use __DEV__ for local dev)
 */
export const API_BASE_URL = __DEV__
  ? "http://localhost:8000/api/v1"
  : "https://api.roofvision.example.com/api/v1";
